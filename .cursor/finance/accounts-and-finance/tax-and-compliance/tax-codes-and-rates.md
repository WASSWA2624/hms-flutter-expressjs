<!-- Generated by tool/generate_finance_tab_docs.py. Edit the source specification or generator, then regenerate. -->
---
menu: "Accounts & Finance"
submenu: "Tax & Compliance"
tab: "Tax Codes & Rates"
tab_slug: "tax-codes-and-rates"
module_key: "accounts"
route: "/accounts?section=tax-codes-and-rates"
entitlement: "facility-accounts"
source_sections: "9.3 / 10.3"
implementation_status: "target-refactor"
---

# Tax Codes & Rates

## Purpose

Maintains the authoritative tax codes & rates records used by Tax & Compliance. The table supports controlled CRUD, versioning, activation, duplicate prevention, and downstream reference integrity.

This is a target-state specification. Existing Billing, Accounts, or Claims code may implement only a subset; the source-of-truth document and this specification define the intended refactor.

## Existing implementation to reuse

Run the reuse audit in [../../_shared/existing-implementation.md](../../_shared/existing-implementation.md) before writing code. This surface is a refactor of existing code unless the audit proves a genuine gap.

- Accounts workspace and its existing sections (open work, to post, approvals, general ledger, patient ledgers, chart of accounts, invoices, fiscal years & periods): `frontend/lib/features/accounts/presentation/pages/accounts_workspace_page.dart`
- Accounts backend: `backend/src/modules/accounts-workspace/`, `backend/src/modules/accounts-invoice/`, `backend/src/modules/chart-account/`
- Currency defaults, formatting, precision, and conversion: `frontend/lib/shared/components/app_currency.dart`, `frontend/lib/core/currency/effective_default_currency_provider.dart`, `frontend/lib/core/currency/fx_currency_utils.dart`, `frontend/lib/shared/components/app_currency_amount_field.dart`, `frontend/lib/shared/components/app_currency_select_field.dart`
- Workspace shell, tables, dialogs, forms, toolbars, and print: `frontend/lib/shared/components/`, `frontend/lib/shared/layout/`
- Permissions, entitlement, ABAC, and access gating: `frontend/lib/core/permissions/`, `backend/src/modules/abac-policy/`, `backend/src/modules/permission/`
- Report definitions, runs, schedules, and exports: `backend/src/modules/report-definition/`, `backend/src/modules/report-run/`, `backend/src/modules/report-schedule/`, `backend/src/modules/reports-workspace/`, `frontend/lib/features/reports/presentation/pages/reports_workspace_page.dart`
- Audit, change, and PHI-access history: `backend/src/modules/audit-log/`, `backend/src/modules/system-change-log/`, `backend/src/modules/phi-access-log/`

Extend the owning module in place. Do not add a parallel model, route family, controller, panel, permission key, or formatter for a record something above already owns, and do not create a second source of truth for it.

## Navigation and workspace placement

- **Menu path:** `Accounts & Finance → Tax & Compliance → Tax Codes & Rates`
- **Canonical URL:** `/accounts?section=tax-codes-and-rates`
- **Surface kind:** permanent `master` table/worklist **tab**, inside the `Tax & Compliance` menu item
- **Reopen behavior:** focus the existing surface and preserve search, filters, sort, page, and selected columns
- **Parent behavior:** The `Accounts & Finance` sidebar entry expands one level into its category menu items; `Tax & Compliance` opens the workspace, and this tab is one of that category's tabs
- **Forbidden:** a second menu nesting level, a category `AppTabStrip` above the section strip, or `AppTabStripVariant.nested`
- **Navigation contract:** [../../_shared/navigation-model.md](../../_shared/navigation-model.md)
- **Source:** [billing-accounts-finance.md](../../../billing-accounts-finance.md) §§9.3 / 10.3

## Pharmacy-guided UI implementation

Follow the `/pharmacy` workspace conventions:

- `AsyncStateScaffold` → `ResponsivePage` → `AppWorkspace`
- One flat `AppTabStrip` (`frontend/lib/shared/components/app_tab_strip.dart`) holding the sections of the active category; the category itself is a sidebar menu item, not a tab
- `AppListTable` for the primary table, server pagination, saved columns, export, and print
- Row click opens `showAppDialog` / `AppDialog`; sections use `AppWorkspaceDetailPanel`
- Mutations use `AppWorkspaceMutationDialog` or `AppDialog`, not permanent create/edit tabs
- `AppAccessActionGate` hides unauthorized buttons; unavailable menu items and tabs are omitted
- `syncWorkspaceLocation` keeps the section slug, filters, and deep links synchronized
- The active surface's count badge comes from the filtered server total; sibling badge counts come from the workspace summary

See [../../_shared/workspace-pattern.md](../../_shared/workspace-pattern.md) for exact Pharmacy references and the target extraction pattern.

## Primary table columns

The baseline table columns and behavior in [../../_shared/table-contract.md](../../_shared/table-contract.md) are additive to the columns below.

| # | Column ID | Label | Type | Default | Sortable | Filterable | Display behavior |
|---:|---|---|---|:---:|:---:|:---:|---|
| 1 | `tax_code` | Tax Code | reference | Yes | Yes | No | Human-friendly reference; opens the authoritative detail. |
| 2 | `tax_name` | Tax Name | money | Yes | Yes | Yes | Right-aligned; currency precision; subtotal when meaningful. |
| 3 | `tax_type` | Tax Type | money | Yes | Yes | Yes | Right-aligned; currency precision; subtotal when meaningful. |
| 4 | `jurisdiction` | Jurisdiction | text | Yes | Yes | No | Localized text; null values render as an em dash. |
| 5 | `rate` | Rate | percentage | Yes | Yes | No | Right-aligned percentage with configured precision. |
| 6 | `inclusive_and_exclusive` | Inclusive/Exclusive | text | Yes | Yes | No | Localized text; null values render as an em dash. |
| 7 | `recoverable_percentage` | Recoverable Percentage | percentage | Yes | Yes | No | Right-aligned percentage with configured precision. |
| 8 | `input_tax_account` | Input Tax Account | reference | Yes | Yes | Yes | Human-friendly reference; opens the authoritative detail. |
| 9 | `output_tax_account` | Output Tax Account | reference | Yes | Yes | Yes | Human-friendly reference; opens the authoritative detail. |
| 10 | `payable_account` | Payable Account | reference | Yes | Yes | Yes | Human-friendly reference; opens the authoritative detail. |
| 11 | `effective_from` | Effective From | date | Optional | Yes | No | Localized display; API remains ISO-8601; date filter enabled. |
| 12 | `effective_to` | Effective To | date | Optional | Yes | No | Localized display; API remains ISO-8601; date filter enabled. |
| 13 | `filing_category` | Filing Category | enum | Optional | Yes | Yes | Localized text; null values render as an em dash. |
| 14 | `tax_authority` | Tax Authority | money | Optional | Yes | No | Right-aligned; currency precision; subtotal when meaningful. |
| 15 | `status` | Status | status | Yes | Yes | Yes | Localized `AppWorkspaceStatusBadge`; never expose raw enum text. |

### Column defaults

- Freeze the first identifying reference and primary party/name columns.
- Default sort uses the most relevant business date descending, then human-friendly reference descending.
- Monetary totals are computed for the filtered result on the server, not only the visible page.
- Optional columns remain available in the column picker and exports.
- PHI and financial amounts are masked or omitted when ABAC scope does not permit them.

## Filters

| Filter | Control | Default | Behavior |
|---|---|---|---|
| Search | server text search | blank | Matches human-friendly reference and permitted display fields. |
| Status | multi-select | open/active | Only statuses applicable to this tab are offered. |

### Toolbar buttons

| Button | Permission | Enabled when | Result |
|---|---|---|---|
| New record | `accounts:write` ∩ `facility-accounts` | write permitted | Opens the Tax Codes & Rates create dialog. |
| Refresh | `accounts:read` ∩ `facility-accounts` | always | Targeted refresh of rows and count badge. |
| Filters | `accounts:read` ∩ `facility-accounts` | always | Opens advanced filter group. |
| Columns | `accounts:read` ∩ `facility-accounts` | always | Opens column visibility and ordering settings. |
| Export | `accounts:read` ∩ `evidence:export` ∩ `facility-accounts` | rows visible | Exports matching rows and records an audit event. |
| Print | `accounts:read` ∩ `evidence:export` ∩ `facility-accounts` | rows visible | Opens print preview using matching rows. |

### Row buttons

| Button | Permission | Enabled when | Result |
|---|---|---|---|
| View | `accounts:read` ∩ `facility-accounts` | always | Opens a detail `AppDialog` using the human-friendly reference. |
| Edit | `accounts:write` ∩ `facility-accounts` | record active/draft and version current | Opens prefilled edit dialog. |
| Clone | `accounts:write` ∩ `facility-accounts` | record clonable | Creates an unsaved copy with a new reference. |
| Activate / deactivate | `accounts:write` ∩ `facility-accounts` | transition allowed | Requires confirmation and refreshes dependants. |
| Archive / restore | `accounts:write` ∩ `facility-accounts` | not referenced by a blocking workflow | Soft archive only; never hard-delete referenced data. |

### Bulk buttons

| Button | Permission | Behavior |
|---|---|---|
| Activate / deactivate selected | `accounts:write` ∩ `facility-accounts` | Only compatible, unblocked records are changed. |
| Archive selected | `accounts:write` ∩ `facility-accounts` | Soft archive; partial failures return per-row reasons. |
| Export selected | `accounts:read` ∩ `evidence:export` ∩ `facility-accounts` | Exports selected rows in visible-column order. |

## CRUD and workflow contract

| Capability | Supported | Rules |
|---|:---:|---|
| Create | Yes | Opens a dialog; server assigns the human-friendly reference and safe initial status. |
| Read/list | Yes | Server-side paginated and ABAC-scoped. |
| Update | Yes | Master records use optimistic versioning; operational records are editable only before posting/finalization. |
| Delete | Conditional | Master data uses soft archive; draft-only administrative delete may be allowed. Posted financial records are never hard-deleted. |
| Approve | N/A | Not applicable; approval remains on the authoritative source workflow. |
| Post/process | N/A | Not applicable; this tab reads authoritative posted/derived data. |
| Reverse/void | N/A | Not applicable; reverse or void the authoritative source record. |

## Create/edit dialog

| Field | Control/type | Required | Validation |
|---|---|:---:|---|
| Tax Code | reference | Yes | Existing visible human-friendly reference. |
| Tax Name | money | Yes | Non-negative; currency required; configured precision. |
| Tax Type | money | Yes | Non-negative; currency required; configured precision. |
| Jurisdiction | text | Yes | Trimmed; length and domain constraints enforced. |
| Rate | percentage | Conditional | Within configured percentage range. |
| Inclusive/Exclusive | text | Conditional | Trimmed; length and domain constraints enforced. |
| Recoverable Percentage | percentage | Conditional | Within configured percentage range. |
| Input Tax Account | reference | Conditional | Existing visible human-friendly reference. |
| Output Tax Account | reference | Conditional | Existing visible human-friendly reference. |
| Payable Account | reference | Conditional | Existing visible human-friendly reference. |

- The backend assigns the human-friendly reference and initial status.
- Add/remove line-item controls use a nested `AppListTable` where the record has lines.
- Save draft and submit are separate actions; closing with unsaved changes requires confirmation.

## Detail dialog and nested tables

| Section | Content / nested columns | Behavior |
|---|---|---|
| Summary | Key table columns and current workflow status | Uses `AppWorkspaceDetailPanel`. |
| Related records | Authoritative cross-module references | Links open owning-module tabs. |
| Attachments | Document type, file name, uploaded by/at | Permission-gated download/preview. |
| Activity & audit | Event, actor, timestamp, reason, correlation ID | Immutable timeline table. |

All detail sections are collapsible `AppWorkspaceDetailPanel` instances. Mutations refresh the affected row, open detail, workspace counts, and shell badge without reloading unrelated tabs.

## Status model

| Status | Meaning | Allowed transition |
|---|---|---|
| Draft | Not available to downstream workflows. | → Active / Archived |
| Active | Selectable by permitted workflows. | → Inactive / Archived |
| Inactive | Retained but not selectable for new work. | → Active / Archived |
| Archived | Soft-deleted historical record. | → Active when restore is safe |

Raw backend enums must map to localized hospital language and `AppWorkspaceStatusBadge` tones.

## Permissions and entitlement

| Capability | Effective requirement |
|---|---|
| Route/tab/read | `accounts:read` ∩ `facility-accounts` |
| Create/update/process | `accounts:write` ∩ `facility-accounts` |
| Approve/reject/finalize | `accounts:write` ∩ `financial:approve` ∩ `facility-accounts` |
| Export/print | `accounts:read` ∩ `evidence:export` ∩ `facility-accounts` |


Backend authorization is authoritative. Effective access is the union of grants intersected with subscription, assigned module, and ABAC scope. Denied menu entries and buttons are omitted rather than displayed as unusable chrome.

## Target API contract

| Operation | Method and target path | Notes |
|---|---|---|
| List | `GET /api/v1/accounts/work-items?section=tax-codes-and-rates` | `page`, `limit`, `sort_by`, `order`, search, filters; paginated `data` + `meta`. |
| Detail / drill-down | `GET /api/v1/accounts/tax-codes-and-rates/{human_friendly_id}` | Uses public human-friendly identifier or a stable report row key. |
| Create | `POST /api/v1/accounts/tax-codes-and-rates` | Validates duplicate/similarity rules and assigns version metadata. |
| Update | `PUT /api/v1/accounts/tax-codes-and-rates/{human_friendly_id}` | Optimistic `version`; `409` returns current record metadata. |
| Activate/archive | `POST /api/v1/accounts/tax-codes-and-rates/{human_friendly_id}/{action}` | Uses activate, deactivate, archive, or restore with an audit reason. |

- JSON fields are `snake_case`; DTOs map explicitly to Dart domain entities.
- Success responses use `success`, `data`, and `meta`; errors use one safe problem-details shape with a stable code.
- Online-only financial actions are never queued offline.
- Workspace events are reconciliation hints; the client performs a targeted authoritative refresh.

## Validation

- Client and server validate required values, lengths, formats, date ordering, and optimistic record version.
- Backend revalidates tenant, facility, department, ownership, entitlement, permission, and current workflow state.
- Public requests and responses use human-friendly identifiers; raw database IDs never appear.
- Duplicate submission is prevented with an idempotency key for financial mutations.
- Line totals, discounts, taxes, payer responsibility, and document totals are recalculated server-side.
- Posting, close, reversal, and adjustment actions are blocked when the fiscal period is locked.

Warnings are visibly distinct from blocking errors. Override actions require an allowed role, a reason, and an audit event.

## Audit and observability

- Log create, update, submit, approve/reject, post/process, allocation, reconciliation, reverse/void, archive/restore, export, and print events that apply.
- Capture actor, role, tenant/facility scope, timestamp, before/after summary, reason, source reference, version, IP/device context, and correlation ID.
- Never include secrets or unnecessary PHI in logs, errors, analytics, or export audit payloads.
- Record latency, result count, failures, and retry outcomes without exposing row data.

## Refactor work breakdown

0. **Reuse audit:** complete the five steps in [../../_shared/existing-implementation.md](../../_shared/existing-implementation.md) and record which of the steps below collapse into extending existing code.
1. **Navigation:** add `tax-codes-and-rates` to the workspace section enum/query parser, canonical slug map, localized label, permission filter, and count-badge mapper. Add it to its category's flat tab strip; the category is the sidebar menu item and must not become a tab row.
2. **Presentation:** extract `frontend/lib/features/accounts/presentation/widgets/accounts_tax_codes_and_rates_panel.dart`; compose it from `AppListTable` and shared workspace components.
3. **Table support:** define typed column IDs, default/optional columns, server sort keys, filters, export mapping, print mapping, empty/error states, and count semantics.
4. **Details and CRUD:** add focused detail and mutation dialogs; include the nested tables and buttons in this specification.
5. **State:** extend the workspace query/state/controller with a paginated result for this tab and targeted mutation refresh.
6. **Data layer:** add DTOs and explicit snake_case mappings, repository methods, optimistic version handling, and problem-details errors.
7. **Backend:** add schema, route, controller, service, and repository support under the target workspace; keep workflow transitions and postings transactional.
8. **Access:** add a tab/action atom matrix to the module access file using existing canonical permission keys.
9. **Tests:** cover route restoration, tab visibility, columns, filters, pagination, CRUD/action gates, statuses, export/print, detail nesting, refresh, and source-document links.
10. **Migration:** preserve existing compatible deep-link aliases, map legacy records/statuses explicitly, and remove old tab code only after parity tests pass.

## Implementation references

- Target page: `frontend/lib/features/accounts/presentation/pages/accounts_workspace_page.dart`
- Target access matrix: `frontend/lib/features/accounts/presentation/accounts_access.dart`
- Target backend routes: `backend/src/modules/accounts-workspace/routes/accounts-workspace.routes.js`
- Pharmacy page exemplar: `frontend/lib/features/pharmacy/presentation/pages/pharmacy_workspace_page.dart`
- Pharmacy tab mapping: `frontend/lib/features/pharmacy/presentation/widgets/pharmacy_scope_navigation.dart`
- Pharmacy access atoms: `frontend/lib/features/pharmacy/presentation/pharmacy_access.dart`
- Shared contracts: [../../_shared/README.md](../../_shared/README.md)

## Acceptance criteria

- [ ] The reuse audit ran and its outcome is recorded; no parallel model, route, panel, permission key, or formatter was added for something already owned elsewhere.
- [ ] `Accounts & Finance → Tax & Compliance` resolves through exactly two sidebar menu levels, and this tab appears in the category's single flat `AppTabStrip` with no category tab row and no nested variant.
- [ ] The surface appears at `Accounts & Finance → Tax & Compliance → Tax Codes & Rates` only with the required entitlement and read access.
- [ ] Canonical section slug `tax-codes-and-rates` deep-links and restores the selected surface.
- [ ] Primary `AppListTable` exposes every tab-specific and applicable baseline column.
- [ ] Search, filters, server sort, pagination, column chooser, saved views, export, and print work.
- [ ] Buttons match this specification and are hidden when permission, status, or scope denies them.
- [ ] Create/edit and workflow operations use dialogs or contextual details, not permanent action tabs.
- [ ] Detail view contains related nested tables, attachments, and immutable audit history.
- [ ] Financial totals, count badges, and active filtered totals come from the server.
- [ ] Public UI/API/realtime references use human-friendly IDs and localized labels.
- [ ] Mutations refresh the affected row, detail, counts, and shell badge.
- [ ] Posted/finalized history cannot be edited or hard-deleted; corrections are linked and auditable.
- [ ] Empty, loading, partial, forbidden, conflict, and retryable error states follow shared workspace behavior.
