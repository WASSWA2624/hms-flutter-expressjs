<!-- Generated by tool/generate_finance_tab_docs.py. Edit the source specification or generator, then regenerate. -->
---
menu: "Insurance & Claims"
submenu: ""
tab: "Insurer Payments"
tab_slug: "insurer-payments"
module_key: "claims"
route: "/claims?section=insurer-payments"
entitlement: "insurance-claims"
source_sections: "9.4 / 10.4"
implementation_status: "target-refactor"
---

# Insurer Payments

## Purpose

Manages the end-to-end insurer payments workflow for Insurance & Claims. The table is the authoritative work queue for creation, review, approval, posting or processing, exception handling, and audit.

This is a target-state specification. Existing Billing, Accounts, or Claims code may implement only a subset; the source-of-truth document and this specification define the intended refactor.

## Navigation and workspace placement

- **Menu path:** `Insurance & Claims → Insurer Payments`
- **Canonical URL:** `/claims?section=insurer-payments`
- **Tab kind:** permanent `transaction` table/worklist
- **Reopen behavior:** focus the existing tab and preserve search, filters, sort, page, and selected columns
- **Parent behavior:** The Insurance & Claims menu opens this flat workspace tab
- **Source:** [billing-accounts-finance.md](../../billing-accounts-finance.md) §§9.4 / 10.4

## Pharmacy-guided UI implementation

Follow the `/pharmacy` workspace conventions:

- `AsyncStateScaffold` → `ResponsivePage` → `AppWorkspace`
- `AppTabStrip` for Billing and Insurance tabs; Accounts leaf submenu selection opens the same tab model
- `AppListTable` for the primary table, server pagination, saved columns, export, and print
- Row click opens `showAppDialog` / `AppDialog`; sections use `AppWorkspaceDetailPanel`
- Mutations use `AppWorkspaceMutationDialog` or `AppDialog`, not permanent create/edit tabs
- `AppAccessActionGate` hides unauthorized buttons; unavailable menu/tabs are omitted
- `syncWorkspaceLocation` keeps the section slug, filters, and deep links synchronized
- Active tab count comes from the filtered server total; inactive badge counts come from the workspace summary

See [../_shared/workspace-pattern.md](../_shared/workspace-pattern.md) for exact Pharmacy references and the target extraction pattern.

## Primary table columns

The baseline table columns and behavior in [../_shared/table-contract.md](../_shared/table-contract.md) are additive to the columns below.

| # | Column ID | Label | Type | Default | Sortable | Filterable | Display behavior |
|---:|---|---|---|:---:|:---:|:---:|---|
| 1 | `payment_no` | Payment No. | reference | Yes | Yes | No | Human-friendly reference; opens the authoritative detail. |
| 2 | `payment_date` | Payment Date | date | Yes | Yes | Yes | Localized display; API remains ISO-8601; date filter enabled. |
| 3 | `payer` | Payer | text | Yes | Yes | Yes | Searchable display value; opens permitted source context. |
| 4 | `remittance_no` | Remittance No. | reference | Yes | Yes | No | Human-friendly reference; opens the authoritative detail. |
| 5 | `payment_method` | Payment Method | enum | Yes | Yes | Yes | Localized text; null values render as an em dash. |
| 6 | `bank_account` | Bank Account | reference | Yes | Yes | Yes | Human-friendly reference; opens the authoritative detail. |
| 7 | `external_reference` | External Reference | reference | Yes | Yes | Yes | Human-friendly reference; opens the authoritative detail. |
| 8 | `gross_amount` | Gross Amount | money | Yes | Yes | No | Right-aligned; currency precision; subtotal when meaningful. |
| 9 | `deductions` | Deductions | money | Yes | Yes | No | Right-aligned; currency precision; subtotal when meaningful. |
| 10 | `net_amount` | Net Amount | money | Yes | Yes | No | Right-aligned; currency precision; subtotal when meaningful. |
| 11 | `allocated_amount` | Allocated Amount | money | Optional | Yes | No | Right-aligned; currency precision; subtotal when meaningful. |
| 12 | `unallocated_amount` | Unallocated Amount | money | Optional | Yes | No | Right-aligned; currency precision; subtotal when meaningful. |
| 13 | `currency` | Currency | currency | Optional | Yes | Yes | Localized text; null values render as an em dash. |
| 14 | `value_date` | Value Date | date | Optional | Yes | Yes | Localized display; API remains ISO-8601; date filter enabled. |
| 15 | `journal_entry_no` | Journal Entry No. | reference | Optional | Yes | No | Human-friendly reference; opens the authoritative detail. |
| 16 | `reconciliation_status` | Reconciliation Status | status | Yes | Yes | Yes | Localized `AppWorkspaceStatusBadge`; never expose raw enum text. |
| 17 | `payment_status` | Payment Status | status | Yes | Yes | Yes | Localized `AppWorkspaceStatusBadge`; never expose raw enum text. |

### Column defaults

- Freeze the first identifying reference and primary party/name columns.
- Default sort uses the most relevant business date descending, then human-friendly reference descending.
- Monetary totals are computed for the filtered result on the server, not only the visible page.
- Optional columns remain available in the column picker and exports.
- PHI and financial amounts are masked or omitted when ABAC scope does not permit them.

## Filters

| Filter | Control | Default | Behavior |
|---|---|---|---|
| Search | server text search | blank | Matches human-friendly reference and permitted display fields. |
| Status | multi-select | open/active | Only statuses applicable to this tab are offered. |
| Date/period | date range or fiscal period | current period | Uses facility timezone and inclusive boundaries. |
| Party | async search-select | all | Searches only parties visible in the user's ABAC scope. |
| Currency | select | base currency | Original-currency values remain available. |
| Amount range | minimum / maximum money | blank | Requires currency when mixed-currency rows are present. |

### Toolbar buttons

| Button | Permission | Enabled when | Result |
|---|---|---|---|
| Record payment | `billing:write` ∩ `insurance-claims` | write permitted | Opens payment dialog and returns the created row. |
| Refresh | `claims:read` ∩ `insurance-claims` | always | Targeted refresh of rows and count badge. |
| Filters | `claims:read` ∩ `insurance-claims` | always | Opens advanced filter group. |
| Columns | `claims:read` ∩ `insurance-claims` | always | Opens column visibility and ordering settings. |
| Export | `claims:read` ∩ `evidence:export` ∩ `insurance-claims` | rows visible | Exports matching rows and records an audit event. |
| Print | `claims:read` ∩ `evidence:export` ∩ `insurance-claims` | rows visible | Opens print preview using matching rows. |

### Row buttons

| Button | Permission | Enabled when | Result |
|---|---|---|---|
| View | `claims:read` ∩ `insurance-claims` | always | Opens a detail `AppDialog` using the human-friendly reference. |
| Edit draft | `billing:write` ∩ `insurance-claims` | status is draft/returned | Opens mutation dialog with optimistic version. |
| Submit | `billing:write` ∩ `insurance-claims` | validation passes | Moves record to approval/review queue. |
| Approve / reject | `billing:write` ∩ `financial:approve` ∩ `insurance-claims` | pending approval and maker-checker satisfied | Records decision and reason. |
| Post / process | `billing:write` ∩ `insurance-claims` | approved and period/workflow open | Runs atomic mutation and refreshes linked modules. |
| Reverse / void | `billing:write` ∩ `financial:approve` ∩ `insurance-claims` | posted/processed and policy allows | Reason required; creates linked reversal, never deletes history. |
| Print | `claims:read` ∩ `evidence:export` ∩ `insurance-claims` | formal document available | Opens document-template preview. |

### Bulk buttons

| Button | Permission | Behavior |
|---|---|---|
| Submit selected | `billing:write` ∩ `insurance-claims` | Only valid draft rows are submitted. |
| Approve selected | `billing:write` ∩ `financial:approve` ∩ `insurance-claims` | Maker-checker and homogeneous status required. |
| Post/process selected | `billing:write` ∩ `insurance-claims` | Atomic per record; failures do not hide successful rows. |
| Export selected | `claims:read` ∩ `evidence:export` ∩ `insurance-claims` | Audit event records filters and row count. |

## CRUD and workflow contract

| Capability | Supported | Rules |
|---|:---:|---|
| Create | Yes | Opens a dialog; server assigns the human-friendly reference and safe initial status. |
| Read/list | Yes | Server-side paginated and ABAC-scoped. |
| Update | Yes | Master records use optimistic versioning; operational records are editable only before posting/finalization. |
| Delete | Conditional | Master data uses soft archive; draft-only administrative delete may be allowed. Posted financial records are never hard-deleted. |
| Approve | Yes | Maker-checker: creator cannot approve when segregation rules require independence. |
| Post/process | Yes | Atomic service transaction; period, entitlement, permission, and source-state checks are repeated server-side. |
| Reverse/void | Yes | Dedicated POST action with mandatory reason and linked correction; never overwrite posted history. |

## Create/edit dialog

| Field | Control/type | Required | Validation |
|---|---|:---:|---|
| Payment Date | date | Yes | Valid ISO date; obeys start/end and period rules. |
| Payer | text | Yes | Trimmed; length and domain constraints enforced. |
| Remittance No. | reference | Yes | Existing visible human-friendly reference. |
| Payment Method | enum | Yes | Value from controlled localized options. |
| Bank Account | reference | Conditional | Existing visible human-friendly reference. |
| External Reference | reference | Conditional | Existing visible human-friendly reference. |
| Gross Amount | money | Conditional | Non-negative; currency required; configured precision. |
| Deductions | money | Conditional | Non-negative; currency required; configured precision. |
| Net Amount | money | Conditional | Non-negative; currency required; configured precision. |
| Currency | currency | Conditional | Trimmed; length and domain constraints enforced. |

- The backend assigns the human-friendly reference and initial status.
- Add/remove line-item controls use a nested `AppListTable` where the record has lines.
- Save draft and submit are separate actions; closing with unsaved changes requires confirmation.

## Detail dialog and nested tables

| Section | Content / nested columns | Behavior |
|---|---|---|
| Summary | Method, payer/payee, value date, amount, currency, status | Read-only header. |
| Allocations | Source reference, target invoice/claim, amount, remaining | Nested table with allocation action. |
| Settlement | Cash point/bank, external reference, reconciliation | Cross-links to treasury. |
| Documents & audit | Receipt/remittance files, approvals, reversals | Immutable event history. |

All detail sections are collapsible `AppWorkspaceDetailPanel` instances. Mutations refresh the affected row, open detail, workspace counts, and shell badge without reloading unrelated tabs.

## Status model

| Status | Meaning | Allowed transition |
|---|---|---|
| Draft | Claim-related record is editable. | → Validated / Cancelled |
| Validated | Blocking validation passed. | → Submitted |
| Submitted | Sent to payer. | → In review / Rejected |
| In review | Awaiting payer decision. | → Approved / Partially approved / Denied |
| Approved / Partially approved | Adjudicated amount accepted. | → Paid / Appealed |
| Denied | Payer rejected all or part. | → Resubmitted / Appealed / Closed |
| Paid / Closed | Settlement complete. | Terminal except audited reopening |

Raw backend enums must map to localized hospital language and `AppWorkspaceStatusBadge` tones.

## Permissions and entitlement

| Capability | Effective requirement |
|---|---|
| Route/tab/read | `claims:read` ∩ `insurance-claims` |
| Create/update/process | `billing:write` ∩ `insurance-claims` |
| Approve/reject/finalize | `billing:write` ∩ `financial:approve` ∩ `insurance-claims` |
| Export/print | `claims:read` ∩ `evidence:export` ∩ `insurance-claims` |


> Claims currently reuses `billing:write` for mutations because the canonical permission catalog has no `claims:write`. Add a dedicated key to both backend and frontend catalogs before changing this contract.

Backend authorization is authoritative. Effective access is the union of grants intersected with subscription, assigned module, and ABAC scope. Denied menu entries and buttons are omitted rather than displayed as unusable chrome.

## Target API contract

| Operation | Method and target path | Notes |
|---|---|---|
| List | `GET /api/v1/claims-workspace/work-items?section=insurer-payments` | `page`, `limit`, `sort_by`, `order`, search, filters; paginated `data` + `meta`. |
| Detail / drill-down | `GET /api/v1/claims-workspace/insurer-payments/{human_friendly_id}` | Uses public human-friendly identifier or a stable report row key. |
| Create | `POST /api/v1/claims-workspace/insurer-payments` | Idempotency key required for financial mutations. |
| Update draft | `PUT /api/v1/claims-workspace/insurer-payments/{human_friendly_id}` | Optimistic `version`; only editable workflow states. |
| Workflow action | `POST /api/v1/claims-workspace/insurer-payments/{human_friendly_id}/{action}` | Submit, approve, post/process, allocate, reverse, or void; reason required where applicable. |

- JSON fields are `snake_case`; DTOs map explicitly to Dart domain entities.
- Success responses use `success`, `data`, and `meta`; errors use one safe problem-details shape with a stable code.
- Online-only financial actions are never queued offline.
- Workspace events are reconciliation hints; the client performs a targeted authoritative refresh.

## Validation

- Client and server validate required values, lengths, formats, date ordering, and optimistic record version.
- Backend revalidates tenant, facility, department, ownership, entitlement, permission, and current workflow state.
- Public requests and responses use human-friendly identifiers; raw database IDs never appear.
- Duplicate submission is prevented with an idempotency key for financial mutations.
- Amounts must be positive and cannot exceed the eligible, unallocated, refundable, or open balance.
- Eligibility, coverage, authorization, required documents, deadlines, and payer rules are rechecked server-side.

Warnings are visibly distinct from blocking errors. Override actions require an allowed role, a reason, and an audit event.

## Audit and observability

- Log create, update, submit, approve/reject, post/process, allocation, reconciliation, reverse/void, archive/restore, export, and print events that apply.
- Capture actor, role, tenant/facility scope, timestamp, before/after summary, reason, source reference, version, IP/device context, and correlation ID.
- Never include secrets or unnecessary PHI in logs, errors, analytics, or export audit payloads.
- Record latency, result count, failures, and retry outcomes without exposing row data.

## Refactor work breakdown

1. **Navigation:** add `insurer-payments` to the workspace section enum/query parser, canonical slug map, localized label, permission filter, and count-badge mapper.
2. **Presentation:** extract `frontend/lib/features/claims/presentation/widgets/insurer_payments_tab.dart`; compose it from `AppListTable` and shared workspace components.
3. **Table support:** define typed column IDs, default/optional columns, server sort keys, filters, export mapping, print mapping, empty/error states, and count semantics.
4. **Details and CRUD:** add focused detail and mutation dialogs; include the nested tables and buttons in this specification.
5. **State:** extend the workspace query/state/controller with a paginated result for this tab and targeted mutation refresh.
6. **Data layer:** add DTOs and explicit snake_case mappings, repository methods, optimistic version handling, and problem-details errors.
7. **Backend:** add schema, route, controller, service, and repository support under the target workspace; keep workflow transitions and postings transactional.
8. **Access:** add a tab/action atom matrix to the module access file using existing canonical permission keys.
9. **Tests:** cover route restoration, tab visibility, columns, filters, pagination, CRUD/action gates, statuses, export/print, detail nesting, refresh, and source-document links.
10. **Migration:** preserve existing compatible deep-link aliases, map legacy records/statuses explicitly, and remove old tab code only after parity tests pass.

## Implementation references

- Target page: `frontend/lib/features/claims/presentation/pages/claims_workspace_page.dart`
- Target access matrix: `frontend/lib/features/claims/presentation/claims_access.dart`
- Target backend routes: `backend/src/modules/claims-workspace/routes/claims-workspace.routes.js`
- Pharmacy page exemplar: `frontend/lib/features/pharmacy/presentation/pages/pharmacy_workspace_page.dart`
- Pharmacy tab mapping: `frontend/lib/features/pharmacy/presentation/widgets/pharmacy_scope_navigation.dart`
- Pharmacy access atoms: `frontend/lib/features/pharmacy/presentation/pharmacy_access.dart`
- Shared contracts: [../_shared/README.md](../_shared/README.md)

## Acceptance criteria

- [ ] Tab appears at `Insurance & Claims → Insurer Payments` only with the required entitlement and read access.
- [ ] Canonical section slug `insurer-payments` deep-links and restores the selected tab.
- [ ] Primary `AppListTable` exposes every tab-specific and applicable baseline column.
- [ ] Search, filters, server sort, pagination, column chooser, saved views, export, and print work.
- [ ] Buttons match this specification and are hidden when permission, status, or scope denies them.
- [ ] Create/edit and workflow operations use dialogs or contextual details, not permanent action tabs.
- [ ] Detail view contains related nested tables, attachments, and immutable audit history.
- [ ] Financial totals, count badges, and active filtered totals come from the server.
- [ ] Public UI/API/realtime references use human-friendly IDs and localized labels.
- [ ] Mutations refresh the affected row, detail, counts, and shell badge.
- [ ] Posted/finalized history cannot be edited or hard-deleted; corrections are linked and auditable.
- [ ] Empty, loading, partial, forbidden, conflict, and retryable error states follow shared workspace behavior.
