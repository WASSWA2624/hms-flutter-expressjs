---
alwaysApply: true
---
Whenever You are loading data or maybe saving something or doing anything make sure that the loading indicator is implemented appropriately and it has the relevant messages. it should do actually display nicely so the loading indi there is a loading indicator for the UI, for example, when content i-i-in the UI is changing or maybe when you're switching from one part of the UI to another or when you are loading data, and then there's also loading indicators on buttons, prefer loading indicators that For that, that, that use the, that, that use the predefined loading indicator which uses the, the logo. for the button, that's only when an action is being done, but it is not routing or changing the interface broadly. then also make sure that whenever you're implementing something that affects the back end and the front end, make sure that the front end is also the two synchronized so always check to confirm for conflicts and then always check that Role-based access and action-based access are implemented for the different available permissions, for all the permissions that are available within the app, make sure that access is implemented. So this rule should always be referenced whenever a piece of code is modified.