When a new screen is being loaded, there is a loading indicator that comes as a circular spinner. I don't like that to be. I want to remove that loading spinner and instead introduce a defined A loading indicator, which is a very narrow horizontal line, immediately below the app title bar, that is, section which has the, which has the hospital logo and app name, which has the app logo and app name, then the toggle sidebar and some other action buttons like online full screen notifications and account. So just immediately, it should be a thin, narrow but visible indicator which runs from left to right, left to right, to show that there is something being loaded instead of having that loading spinner, you know, when content is changing on the screen from The current screen to the next screen. That moment of seeing an empty screen isn't good. So I want the previous screen to disappear when the new screen has already loaded. So I think that would create, would create good user experience. Also, in the app bar, in the app bar, that the online/offline indicator, I don't know why it is gray when the app is online. And when I turn off internet, I really don't see any visible change. So it should be in such a way that when internet is off it turns red with the app cross. Something so that internet is off and connection is off, and when connection is on, then it should turn green. Green, it should show green. So let's make that change and get rid of the already, get rid of all the code that defines that warning indicator so that we now have that horizontal warning indicator, thin one but visible on all screens and all, all screen sizes and all platforms.