// Temporary compile stub for Accounts GL deliverable.
import 'package:flutter/widgets.dart';

/// Placeholder so unfinished sibling desks do not break GL compilation.
class AccountsDeskStub extends StatelessWidget {
  const AccountsDeskStub({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
