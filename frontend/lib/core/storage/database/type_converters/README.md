# Drift Type Converters

Place reusable Drift `TypeConverter` implementations here when a schema needs
to persist value objects or platform-neutral primitives.

The initial schema uses Drift's built-in enum converters for sync queue
operation and status fields, so no custom converter is required yet.
