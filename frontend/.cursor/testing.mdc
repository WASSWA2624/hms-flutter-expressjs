---
alwaysApply: false
globs:
  - "frontend/lib/**"
  - "frontend/test/**"
description: >
  Use this rule file when implementing, reviewing, or refactoring tests in the HOSSPI HMS Flutter frontend. Covers unit, widget, integration, end-to-end, and structure/testing patterns for quality and maintainability.
---

# Testing Strategy

## Scope
Defines the minimum testing structure for the reusable app foundation.

## Mandatory rules
- Use `flutter_test` for unit and widget tests.
- Use `integration_test` for startup, navigation, and important flows.
- Use `patrol` (via `patrol_test/`) for end-to-end module workspace flows and failure diagnostics.
- Use `mocktail` or provider overrides for repository/data-source tests.
- Keep tests close to source structure where practical.
- Test controllers, value objects, mappers, validators, and shared components.
- Test responsive behavior for important shared layouts and page shells.
- For UI sync changes, test local success patching, cancel/failure behavior, typed
  realtime delta or targeted refresh, event-group membership, and reconnect refresh.
- Cross-stack shared mutations require backend event-emission coverage and a
  simulated peer-client reconciliation test.
- Tests must not depend on real production services.
- Tests must not require secrets.

## Test types
| Test type | Target |
|---|---|
| Unit | validators, mappers, use cases, value objects |
| Widget | components, pages, responsive layout, forms |
| Integration | startup, routing, auth shell, platform-critical flows |
| Patrol E2E | module workspace shells, cross-module navigation, failure diagnostics |
| Golden | optional, for stable shared UI only |

## Acceptance checklist
- `flutter test` passes.
- Important components have widget tests.
- Providers can be overridden in tests.
- Responsive layout utilities have tests.

## Related rules
- [`ci_cd_quality_gates.mdc`](./ci_cd_quality_gates.mdc)
- [`state_management.mdc`](./state_management.mdc)
- [`instant_ui_sync.mdc`](./instant_ui_sync.mdc)
- [`components.mdc`](./components.mdc)
- [`layouts.mdc`](./layouts.mdc)
