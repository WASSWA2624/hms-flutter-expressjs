# Patients inventory — convention gaps

Required compliance gaps vs `prompts/.cursor/*.mdc` after shared-chrome + main-tab table remediation.

## Residual

None — main-tab viewport (`scrollable: false` / `Expanded` / no `shrinkWrap`), atomic patient/visit cells, Settings Apply/Reset l10n, Export `exportValue` for next action, Print preview copy via `commonPrint*`, prefer-5 promotion when Admitted Visit is RBAC-omitted, filtered active-tab counts without page-length fallback, and theme-token report preview colors are closed.
