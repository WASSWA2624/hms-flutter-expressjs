# Patients inventory — convention gaps

Required compliance gaps vs `prompts/.cursor/*.mdc` after shared-chrome remediation.

## Residual

None — shared chrome closed table Print (preview-first), Filters/`Advanced filters` common labels, Advanced filters **Close**, active-tab filtered counts, and Export RBAC (`evidence:export`).

Per-tab inventory refresh continues under `prompts/02-patients/01-all.md` … `04-balance-due.md`.
