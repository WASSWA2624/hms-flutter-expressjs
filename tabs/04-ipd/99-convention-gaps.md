# IPD inventory — convention gaps

Required compliance gaps vs `prompts/.cursor/*.mdc` after shared-chrome remediation.

## Residual

None — shared chrome closed table Print (preview-first), Filters/`Advanced filters` common labels, Advanced filters **Close**, follow-ups Filters/date, active-tab filtered counts, Export RBAC (`evidence:export`), and billing-only empty forbidden panel.

Per-tab inventories `tabs/04-ipd/01-admission-queue.md` … `06-follow-ups.md` refreshed to match remediated shared chrome (resolved).
