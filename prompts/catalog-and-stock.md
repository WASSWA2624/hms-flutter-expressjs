So now let us implement. Or improve the catalog and stock. Currently, catalog and stock is existing as a button In the, the search bar. But what I want is convert it into a tab so we should add it to the existing tabs. Currently, when you click it it opens It opens a Drugs, formulary, inventory and storage. Okay, it opened a a dialogue that has drugs, formulary, inventory and storage. And on each one of these ones we have, we have a drug We have the respective buttons. Like for example, under the drugs tab, we have add a drug; under the formulary, we have add a formulary item; then under inventory, we have, we have an add button; and then under storage, we have create room. So what I want is instead of having this as instead of having this as A dialog, we can have it as nested tabs that is nested tab called drugs nested tab called formulary, nested tab called inventory, and another nested tab called story layout. Yes. So I think we can also add another Mr. Tav or Shelf I'm So under each of these tabs, okay, each one of these tabs should render a table, should render a table containing the respective items. For example, for drugs, then it should contain The track. And then for the formulary, it should indicate, it should show the formulary items. And then for the inventory, it should also show the inventory items. And then for the storage layout, it should also indicate the storage layout, and then for the shelf, it also indicates the list of shelves that we have and all the information that is needed for those shelves. So in each table we should have the filter button, the settings button, the export button, and the respective buttons that's under the, the respective nested toolbar Section. For example, under Drugs, we have the Add button, so we should have it there. Then, under Formulary, we have Add Formulary Item, we should add this, we should add it there. Then, under Inventory, we don't have, so it means that, that case would not have any extra button under the setup sec under the, under, under the category. Under Storage, we have the Add Room, and then also under Shelf or Shelves, we have the Add Shelf. So the tables have to be complete. So let's implement that layout. What we want is that instead of having instead of having the catalog and stock as a button within the Within the The many Tabasasachba We should have it as a separately as a separate At a separate tab then we'll have the respective nested tabs that I have listed above. Ensure the tables are comprehensive enough. Use generic action button label such Edit, Delete, Add instead of Add drug, Edit drug, Delete drug, etc.Appropriately space the action buttons. Ensure synchronization of the backend and frontend.