So now let's focus on refining and tightening the of the tabs and the tables. First of all each tab is independent of each other. Usually, currently, we have new orders Pascho? Pending payment, completed, cancelled, all orders. Maybe the first thing you have to change is rename OpaShop to OpaShop Orders so that someone can know That it really means I think this, you, you're sorry, name, completed orders, cancel the orders. So completed should be renamed, right? Completed orders and cancel should be renamed, right? Cancel the orders. And this is the intended behavior and relationship between the easy tabs and their respective tables. So new orders should be a representative of All the orders in the system that haven't yet been Attended it too. So it isn't, it didn't include partial orders, it didn't include pending orders, it didn't include completed orders or canceled orders. It's only those new orders that haven't been attended at all. Okay. Partial orders are those orders that have been partially dispensed. For example For example maybe somebody has ordered for ten tablets, but only eight have been dispensed for some reason, for example, the, the quantities aren't available in the system or for any reason Then pending payments, these are orders that have been tendered to in some way, but they haven't yet cleared the payments. So which means these could be new orders, but maybe the client hasn't yet paid. They could be partial orders, but the client hasn't yet paid, or maybe somebody has paid some money and some money is, so all the money is completed. And completed orders, these are orders that have been completed today on that day. For example the, the payment has been done the quantities have been dispensed, so everything is sorted, so the client is good to go. Then cancel the orders. For some reason, maybe somebody ordered for something, and after maybe for some reason they aren't able to take the product, they can't afford it, they, for any reason, and the quantities weren't dispensed, nothing was dispensed, so this order was placed, maybe a payment won't, won't, won't, won't, won't be made, but or maybe for some reason the attendant canceled the order, so those, those orders should be marked as canceled and only for that day. Then all others, this includes all orders that have been made, whether canceled, whether partial, whether pending, whether completed, whether canceled, whether new whether they were for ten days ago, one year ago, a hundred years ago, all orders in the system, because there are cases where somebody needs to revisit their history. So those orders will be under all orders. So make sure that every count on these orders accurately represents its intended purpose. And so that's how it should be. And every tap should be matched with its filters, and it should be matched with its, the settings exports so they should be matched. Ensure the counts on the tabs are accurate and independent of the other tabs.