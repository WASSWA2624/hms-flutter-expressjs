Can we define a reusable similarity component (Build from collapsible component) under the shared forder and simply reuse it? It should show the parameters being checked, entered values, existing values, percantage similarity per paramters set, overall % similarity, color coded, A use this buttom(right aligned).

Also create a reusable similarity check dialog component with these sections: color coded card on top with apropriate message, Editable collapsible component showing the entered values, which the user can use to retry with this button. A section with similarity cards, a save anyway button in the footer section, cancel button. This dialog utomatically routes to the details dialog of the repsective entry being created.

Ensure elegancy and simplicity.
