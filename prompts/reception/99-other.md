On the Set up, admin set up screen. Let's see, do Let's do the following. There are different types and levels of admins There is Super admin, this is a platform admin who has access to all the, the entire app he has access to The tenants the tenant scopes the facility scopes, and even administrators within within the the, within the facility. So first of all, I want the menu item to be named as either platform setup If you are logged in as a spa admin, then we If you are logged in as a tenant, tenant, tenant setup, then if you are logged in as a facility admin, then you, you see facility setup. Now within, w-we shall also refactor the entire screen such that it uses the tab component So all these, all these items that manage, actually, we should call, we should include things like tenants branches facility, departments, units wards, rooms, beds then we add rules. In that Permissions and users, so all those ones should be specific tabs. All those ones should be specific tabs, and each one of them should consist the respective table components and depending on the user, depending on the user, then we can display the different, the different tables. So first of all if you are logged in as a platform admin, you'll see the tenant, you'll see the tenant Tab. If you are logged in as, as as a tenant, if you're logged in as a tenant, you will see the tenants, you see the tenants tab and all the other tabs. If you're logged in as a tenant, you will see the tenants tab, but within that tenant, you cannot see more than, you cannot see more than the tenant that you belong to. If you're logged in as a facility, then you cannot see the tenants tab, you can see facility and all the other tabs.