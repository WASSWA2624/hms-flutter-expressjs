For each tab. Tab On the Set up on the admin setup screen. And Olde Z Add buttons. Move all these add buttons into the tab store, and the add button should, should be implemented as A primary button in the Dames, duidelijk. Maintain the standings for The tulip bulbs? So the tabs we have are: tenants, branches, facility, departments units, works, rooms, space. Roles, permissions, and users. Now, for each table, for each table, because the, the app is table component has a built-in sidebar, and the each sidebar has a filter and it has a filter button and a settings button, so each table should have a built-in sidebar and filter button, or filter button and the settings button. Then No Nicely add the actions, actions, the respective rows with the respective edit delete buttons. So make the edit and delete buttons generic instead of doing, saying things like edit, ten, ten, ten, edit, maybe delete, ten, ten, ten, make it, delete respective button respective icon.Do this for all the tabs.