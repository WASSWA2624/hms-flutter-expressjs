On the Q actions, the button, I mean, dialog, there is a "Remove" button. this button doesn't actually, it doesn't it's not clear when somebody sees it, it's not clear, and someone can't know what it is used for. So we need to rename it appropriately, as well, so someone can know exactly what it does. And I think also Because when I open it, there are two options. There is two status So when there is a queue status and then there is also such doctor, I think these two can be separated so that at least when somebody's wants to assign another doctor, then they use a separating, they, they use a separating button, I mean dialog, and the assigned doctor should, should be, the assigned doctor button should be able to assign doctor if there isn't a doctor already assigned to this patient, and then if there is a doctor assigned, then it should be able to change doctor, change doctor. So when the respective dialog opens up and then the doctor is changed. But instead of having moved, then we can separate it so that it looks it, it becomes much more clear for someone who is viewing this. So what I, under, under the queue status, I, I'm seeing they are scheduled, confirmed, in progress, completed, and cancelled. So I don't know what these statuses mean. So what does the what does the scheduled confirmed in progress, completed, and cancelled mean? So this one needs to be, I think for these statuses on this dialogue, instead of using only for this sta for this, only for this instead of using the searchable text component, let's use let's use radio buttons so the option and then description that shows what's the respective status means. So when the patient is in progress, what does that mean? When it's scheduled, when it's confirmed, when it's completed, and when it's cancelled, so it becomes clear to someone who's actually Moving one, a patient from one status to another, so it, it should become actually very clear. it should become very clear