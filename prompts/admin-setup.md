On the admin setup screen. Under the tenant tab, the tenant tab So this is what I want When you A tenant. Actually, when you are a, when you have a tenant account, you should literally not be able to add another tenant, but you can manage information about your, your own tenant, your own tenant account. So you should be able to see instead of the add button, you should only be able to see, actually, you are only, you don't, in fact, you don't even see the table, but you instead see the details about your tenant. for example, a tenant name, tenant details, you can edit the tenant name, maybe contact, because a tenant can have a name, can have a lot of details about the tenant. But if you are a super admin, then you should be able to see all the tenants, a list of tenants, you can edit, you can delete you can also add another tenant, so, so you can set up, you can manage your tenants. So that's how, that's what actually should be under the tenants tab.