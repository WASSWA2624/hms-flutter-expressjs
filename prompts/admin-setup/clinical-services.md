Review On the And Under admin setup and under clinical services, and then under the lab tab. There is a create panel button. Which when clicked, it opens the Create Lab Panel Dialog. Yeah, this Create Lab Panel Dialog has a step, a step-wise stepper for For Where we enter the panel details like panel name, panel code, category and description. It's only the panel name which is required, the others are optional. So What I want is that So when I click next, there is the panel tests step where I can search and select the panels, the tests to add on the panel. I think the section for testing the panels, for adding the panels, adding the tests on the panels should be, should use a built-in table with a search bar where you can search And then also we shouldn't have nested scrolling, you know, yeah? the scrolling should be on the entire content within the panel Within the la-the model So when I add, when I select tests, the next step should be the Similarity test the engine which should test, which should check for any similarity for all the parameters that are available on the panel. So it should test for things like panel name panel, panel ID and it, and others. It should check for percentage similarities it should look for tests if a tester has been, has already been added in another panel, and so many others. And for each score, it should attach, it's for each match or for each parameter, it should attach a score. This score should be used to calculate the overall score which shows the similarity. So if anything is similar, then it should give an appropriate rating, percentage similarity value, and also make sure that everything is calibrated. So, so that testing I mean creating a panel should Able to discover even the slightest similarities so that we avoid duplication as much as possible. So Each image should have a button that allows to use this test and after After a successful use this test or create if there is no similarity, then one should, should be routed to the panel details where they can see the details about the panel test that have been added and so on and so forth. So that is the desired behavior.