On the admin setup screen, and then also under that there is clinical services. Tab button Under that there is also another nested labo tab button. So that's under that lab tab buttons, there is a table which contains a list of tabs a list of lab tests and lab panels. Currently, when I open that, when I click on any of the entries in that table, instead of opening a detailed dialog It is, it instead opens an edit dialog. The edit should be opened through the edit button if you are on that table, but the The details should be opened when the details dialog should be opened when I click on any of the entries or rows in the table. For example, if I clicked a test, a lab test, then it should open the specific lab test details dialog. If it was a panel, then it should open the specific lab panel details. If so on those detailed dialog, there will be an edit and a delete button, which when clicked, will invoke the respective edit and delete functionalities of law that is already implemented elsewhere in the lab tab.