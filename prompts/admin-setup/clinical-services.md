Similar to how we created and implemented the create and edit radiology procedures the same should be applied to the laboratory tests and maybe possibly panels, but for now, tests. So before you save a new procedure or edit a procedure, then you must also you must also Perform a comprehensive similarity test and then display the similarity before you commit the changes to the databases so that one is able to know whether this test already exists or not. all role-based access and action-based accesses should be respected.

Also improve the look and feel, plus the overall design. For the Form for the create lab test form. I can see the category option. It's a drop down, isn't it nicely? Styles, it isn't spanning the entire width, but the result kind looks fine. the footer units also isn't looking fine The reference, under the reference ranges, I can see the Fields are so much touching each other, they are not nicely designed, and I think the add reference range button should Should be at The top of the reference range Form fields, I want you to add good intelligence to the reference range so that if you have selected adults, then you cannot add, like, you cannot add duplicate reference ranges. I can see fields that can be replaced with the modal dialogs, but you are still using the regular text fields. So add appropriate validation and make this extremely robust. And the Nice.