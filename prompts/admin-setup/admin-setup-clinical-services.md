So now within the, within the Admin setup under the clinical section. Yeah, so I can see that now for all the tables for radiology, lab, and the diagnosis the The scrolling is okay all the necessary buttons are there and so But still on the diagnosis on the diagnosis' s sub tab they add services that needs to be renamed to add diagnosis so that it actually reflects the meaning, the name. then what I want is to make sure that for other tables, I can sort by any of the columns and instead of only the first column, but I should be able to sort by any of the columns. Yeah, so the sorting also looks nice, but I want also to adjust the table so that The row content nice, is nicely wrapped within the row, and also the sorting is slow, I don't know why it's slow, and the loading of the content within the table when I'm switching from one tab to another takes some few seconds before it updates, so we need to make sure that the loading is very fast so that we have very good user experience.