On the Under clinical service and under admin setup, there is a settings button which when clicked it should do, it should do Open Table column settings dialog for the three tabs, that is radiology, lab, and the diagnosis. So what should be in those columns is all the possible columns for the different, for the different tables radiology, lab, and the diagnosis, so such that one can easily select which column should be visible and which column shouldn't be visible. So implement that for both radiology, lab, and the diagnosis Sub-tabs