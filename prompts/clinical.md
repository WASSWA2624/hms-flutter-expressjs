On the clinical screen. And when I click on a patient there is There is under clinical actions, there is any add diagnosis tab button, which when I open it, it opens the add diagnosis. Dialogue. So the diagnosis can either be primary, secondary, or differential. Right now, there is a diagnosis, there is a diagnosis There is a diagnosis select component, I think we should replace this with we should replace it with radio buttons, so whereby one of the buttons is primary, secondary or differential. So, and arrange those buttons horizontally on the large screens. This dialog should be maximized by default. then remove these buttons which have also says favorite facility and the global catalog, and instead This lefty side, the lefty section which has showing eighty or eighty, should actually show only those diagnoses that were added to the facility, because in the setup, there is a section where there is a tab, there is a clinic tab where we add diagnoses so those, only those which were added to this facility should appear in this list. So one can select one can select from those, and on, on each of them so someone can, can, there is the first column, or the first, the first column is the number column, the second column is the, is the checkbox, so where someone can check and select, so which means someone can add multiple diagnoses on a single patient, and then there is an add button which actually moves the selected ones to the right among the selected ones. So, and so on the right, we shall see we shall see the selected, the, the selected ones, and someone can actually also select and, and move them back to the left section. So one can see which diagnoses have been attached to this patient, and then so still on both tables, so we are saying that there should be there should be, the first column is the number column, the second column is the select column where we check, use the check box select, then the third column is the, the third column is the name of the diagnosis. Then on the right is the same table but on the left we have the add button which adds selections to the right, more selections to the right, and on the right there is a select, a deselect button which removes the, the selected items back to the left. When you click the diagnosis button, then this diagnosis is attached to the patient, the patient who is currently selected for the diagnosis. So the process of adding diagnosis to the table should be very easy and simple. Yes, I can.