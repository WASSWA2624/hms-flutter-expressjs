On the pharmacy dashboard, there is a section called "Most Sold Drugs." What I want us to do is I redefine the section component globally. That is a collapsed section component This collapsible section component can optionally have buttons, action buttons in it. So those action buttons should be located below that area and the table. So for this for this most sold drugs, let's move last month select component, then the tops, for example, top ten, top twenty, and so on values at, and the type of the graph plus the quantity and A mountie, a radio button. I think that she knew Stood by section, such that they don't be align nicely, as is on that So by default most solids. Drugs will be set to today, but then top, the top drugs will be top Five Then the default graph will be line graph, but we can also add support for for the bar chart Then also for the Dell for the, for the For, for the values, the chart value or the summary values, we should use the Quantity by defaults. By default. Yeah, then let's also make sure that for the period we can add a default, I mean, there should be an option for putting a custom value. I know custom values, I think let's have it like that and these drop downs let's design, these select components let's design them ni-nicely so that they look nice on all screen sizes.