# ============================================
# HMS Backend - environment template
# ============================================
# Copy this file to .env, then replace the sample values with the ones for
# your machine or server. Never commit the real .env file to version control.
# Each variable includes guidance for:
# - Development: local machine, LAN/mobile testing, or local QA
# - Production: deployed backend behind a reverse proxy with a public HTTPS frontend

# ============================================
# Required application settings
# ============================================
# MySQL / MariaDB connection string.
# Development: usually localhost with a local database.
# Production: use the database user/password/host for the deployed server.
DATABASE_URL="mysql://root:password@localhost:3306/hms_db"

# Valid values: development, staging, production, test
# Development: "development"
# Production: "production"
NODE_ENV="development"

# JWT signing secret. Must be at least 32 characters.
# Development: generate a local secret for your machine.
# Production: use a strong secret from your server/hosting secrets store.
JWT_SECRET=""

# Allowed frontend origins, comma-separated.
# Development: localhost and LAN frontend URLs.
# Production: public HTTPS frontend URLs only.
CORS_ORIGINS="http://localhost:8081,http://127.0.0.1:8081"

# ============================================
# Server / network
# ============================================
# HTTP API port.
# Development and production commonly use 3000 unless the host requires another port.
PORT="3000"

# Host bind address.
# Development: "0.0.0.0" when phones/tablets on the LAN need access.
# Production: "127.0.0.1" when Nginx/Apache/Caddy proxies public traffic.
HOST="0.0.0.0"

# Express proxy trust.
# Development: usually "false" unless you run through a local proxy.
# Production: usually "1" when one reverse proxy sits in front of Node.
TRUST_PROXY="false"

# Keep true so Ctrl+C or the process manager can stop the server cleanly.
HANDLE_SIGINT="true"

# CORS private-network allowance.
# Development: usually "true" for LAN/mobile testing.
# Production: usually "false" so only explicit public origins are allowed.
ALLOW_PRIVATE_NETWORK_ORIGINS="true"

# ============================================
# Public app URL / branding
# ============================================
# Frontend URL used in emails and auth links.
# Development: localhost frontend URL.
# Production: your public HTTPS app URL.
APP_PUBLIC_URL="http://localhost:8081"

# Application name shown in emails and auth flows.
# Development: default is fine.
# Production: set this to your public-facing brand.
APP_DISPLAY_NAME="Hospital Management System"
APP_SHORT_NAME="HMS"

# JWT access/refresh lifetimes.
# Development and production: keep access tokens short-lived and refresh tokens bounded.
JWT_ACCESS_TOKEN_EXPIRATION="15m"
JWT_REFRESH_TOKEN_EXPIRATION="7d"

# Refresh-session retention window in days.
# Development: 7 is a safe default.
# Production: keep this finite and review alongside your logout/session policies.
AUTH_SESSION_TTL_DAYS="7"

# ============================================
# Email / SMTP
# ============================================
# Leave blank to disable outbound email.
# Development: Mailtrap/local SMTP or blank if you do not need delivery.
# Production: real SMTP provider credentials and verified sender addresses.
SMTP_HOST=""
SMTP_PORT=""
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM=""
SMTP_FROM_NAME="Hospital Management System"
SMTP_REPLY_TO=""
SMTP_NO_REPLY_ADDRESS=""

# Whether registration emails may include a plaintext password.
# Development: can be "true" for temporary local testing if needed.
# Production: keep "false".
ALLOW_PLAINTEXT_PASSWORD_EMAIL="false"

# ============================================
# Security
# ============================================
# Optional CSRF secret. If set, it must be at least 32 characters.
# Development: optional, but use a separate value from production.
# Production: recommended when CSRF-protected flows are enabled.
CSRF_SECRET=""

# Optional AES-256 key (64 hex chars) for encryption-enabled features.
# Development: leave blank unless you are testing encryption flows.
# Production: set a dedicated 64-hex-character key if encryption is enabled.
ENCRYPTION_KEY=""

# ============================================
# Storage
# ============================================
# local or s3
# Development: "local" is simplest.
# Production: use "local" or "s3" depending on your deployment/storage strategy.
STORAGE_PROVIDER="local"

# Required only when STORAGE_PROVIDER="s3"
# Development: usually blank.
# Production: fill with the deployed bucket credentials/region.
AWS_ACCESS_KEY_ID=""
AWS_SECRET_ACCESS_KEY=""
AWS_REGION=""
AWS_S3_BUCKET=""

# ============================================
# WebSocket tuning
# ============================================
# Defaults are safe for local development and modest production loads.
WS_MAX_CONNECTIONS="1000"
WS_HEARTBEAT_INTERVAL="30000"
WS_HEARTBEAT_TIMEOUT="60000"

# ============================================
# Prisma / database pool tuning
# ============================================
# Development: defaults are usually enough.
# Production: tune these to fit your database plan and traffic level.
PRISMA_POOL_CONNECTION_LIMIT="10"
PRISMA_POOL_CONNECT_TIMEOUT_MS="5000"
PRISMA_POOL_ACQUIRE_TIMEOUT_MS="5000"

# MySQL 8 RSA public-key retrieval compatibility toggle.
# Development: often "true" for local MySQL when auth handshakes fail.
# Production: prefer "false" unless your host specifically requires it.
PRISMA_MYSQL_ALLOW_PUBLIC_KEY_RETRIEVAL="true"

# ============================================
# Seeder configuration
# ============================================
# Primarily useful for local/demo data generation, not normal production runtime.
SEED_RECORD_COUNT="50"
SEED_RANDOM_SEED="20260217"

# ============================================
# PACS / DICOMWeb integration
# ============================================
# Leave blank unless radiology/PACS integration is enabled.
# Development: usually blank or pointed at a test PACS.
# Production: use your secured DICOMWeb endpoint and auth mode.
PACS_DICOMWEB_BASE_URL=""
PACS_AUTH_MODE="none"
PACS_USERNAME=""
PACS_PASSWORD=""
PACS_BEARER_TOKEN=""
PACS_TIMEOUT_MS="10000"

# ============================================
# OpenTelemetry / OTLP
# ============================================
# Enable vendor-neutral tracing and metrics export.
# Development: usually "false" unless you are testing with a local collector.
# Production: set to "true" once your OTLP collector endpoint is available.
OTEL_ENABLED="false"

# Logical service name attached to telemetry resources.
# Development: default is fine.
# Production: keep stable for dashboards and alerts.
OTEL_SERVICE_NAME="hms-backend"

# Base OTLP HTTP endpoint for traces and metrics.
# Development: e.g. "http://localhost:4318"
# Production: your collector or observability gateway.
OTEL_EXPORTER_OTLP_ENDPOINT=""

# Metrics export interval in milliseconds.
# Development and production: 60000 is a sensible baseline.
OTEL_METRIC_EXPORT_INTERVAL_MS="60000"

# ============================================
# Feature flags
# ============================================
# Use explicit "true"/"false" to lock a rollout state.
# Development: enable preview workspaces you are actively testing.
# Production: only enable the features you intend to expose.
FEATURE_CLINICAL_WORKBENCH_V2="true"
FEATURE_CLINICAL_ALERT_AUTO_RULES="true"
FEATURE_FOLLOW_UP_REMINDER_DISPATCH="true"
FEATURE_IPD_WORKBENCH_V1="false"
FEATURE_BILLING_WORKSPACE_V1="false"
PHARMACY_WORKSPACE_V1="false"
FEATURE_COMMUNICATIONS_WORKSPACE_V1="true"
FEATURE_SUBSCRIPTIONS_WORKSPACE_V1="true"
FEATURE_DASHBOARD_WORKSPACE_V1="true"
FEATURE_SETTINGS_WORKSPACE_V1="true"
FEATURE_HR_WORKSPACE_V1="true"
FEATURE_HOUSEKEEPING_WORKSPACE_V1="true"
FEATURE_BIOMEDICAL_WORKSPACE_V1="true"
FEATURE_REPORTS_WORKSPACE_V1="true"
RADIOLOGY_ATTESTATION_V2="false"
