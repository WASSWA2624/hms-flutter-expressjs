---
alwaysApply: false
globs:
  - "backend/**"
description: >
  Zod validation for params, query, body; reject raw internal IDs in public inputs.
---
# Validation Rules

Purpose: make request validation deterministic.

Rules:
- every route has explicit Zod schemas for params, query, and body as needed
- schemas use documented `snake_case` field names
- create and update payloads reject unknown keys unless an owner file explicitly allows passthrough metadata
- public route params and body fields accept `human_friendly_id` (and documented business codes) only; reject numeric/uuid internal primary keys in client payloads unless explicitly documented as server-only admin tools
- validate `human_friendly_id` format, tenant scope, and existence before service execution
- enum values, date ranges, and scope inputs must be validated before service execution
- cross-field business rules belong in services only when they need database context
- validation failures return normalized problem-details responses
- validation schemas are part of the required test surface
