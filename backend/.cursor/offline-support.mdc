---
alwaysApply: false
globs:
  - "backend/**"
description: >
  Use this rule file when implementing, designing, or reviewing backend offline support features to ensure sync-capable clients are handled correctly.
---
# Offline Support Rules

Purpose: define server behavior for sync-capable clients.

Rules:
- offline-capable mutations require idempotency and deterministic conflict handling
- list and detail endpoints exposed to offline clients should support incremental sync metadata
- incremental sync metadata (`updated_since`, version/etag fields) must use `human_friendly_id` for record identity in conflict payloads
- after successful online reconciliation, persisted mutations must still emit scoped realtime events per `websockets.mdc`
- auth, entitlement changes, break-glass approval, payments, refunds, mortuary release, and last-office final close are online-only actions
- conflict responses must identify the conflicting record and latest version metadata
- server logic must never assume the client clock is authoritative

See also: [`.cursor/api-contract.mdc`](../../.cursor/api-contract.mdc) and [`frontend/offline_sync.mdc`](../../frontend/.cursor/offline_sync.mdc).
